# Правила проекта (ops)

Зеркало `.cursor/rules/agent-rules.mdc`. Карта — **Grace 2** (`plans/`). Корневого `AGENTS.md` нет — навигация в §2 `agent-rules.mdc`.

## Старт

`docs/HISTORY.md` → `plans/` → фазы Grace 2 (`mode-architect` / `mode-code` / …). Не marketplace `grace-*`.

## Журнал / Git / Среда

Как в `agent-rules.mdc` (HISTORY, VERSION, PowerShell, без локального runtime для проверки).

## `work/`

Отчёты, черновики и прочие вспомогательные артефакты агента — **только** в `work/`; в git не попадают (см. §7 в `agent-rules.mdc`).
