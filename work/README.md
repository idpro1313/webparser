# work/

Каталог для **вспомогательных** артефактов агента: отчёты, черновики, временные выгрузки для анализа.

- Содержимое (кроме этого README) **не коммитится** — см. `.gitignore` и `.cursor/rules/agent-rules.mdc` §7.
- Канон проекта: `plans/`, `docs/`, исходники — не сюда.
